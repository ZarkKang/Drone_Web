version: '3.8'
services:
  web:
    image: node:18-alpine
    working_dir: /app
    volumes:
      - .:/app  # 将当前文件夹“挂载”到容器，实现实时同步
    ports:
      - "3000:3000"
    command: sh -c "npm install && node app.js"